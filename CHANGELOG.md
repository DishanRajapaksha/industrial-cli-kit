# Changelog

## Unreleased

- Add the v1 shared CLI contract, command registry, completion generators, and
  reusable baseline contract tests.
- Add strict YAML parsing, profile-selection, duration, and starter-file helpers.
